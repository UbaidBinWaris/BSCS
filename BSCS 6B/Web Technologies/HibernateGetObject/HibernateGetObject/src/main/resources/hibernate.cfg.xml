<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <!-- <property name="connection.driver_class">org.hsqldb.jdbcDriver</property>
                    <property name="connection.url">jdbc:hsqldb:hsql://localhost/TestDB</property> -->

        <property name="connection.driver_class">org.postgresql.Driver</property>
        <!-- Original configuration with non-existent role -->
        <!--<property name="connection.url">jdbc:postgresql://localhost:5432/spring_modulith</property>-->
        <!--<property name="connection.username">modulitha</property>-->
        <!--<property name="connection.password">modulitha</property>-->

        <!-- Default PostgreSQL configuration - Update with your actual credentials -->
        <property name="connection.url">jdbc:postgresql://localhost:5432/postgres</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">postgres</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <!-- Commented out as Hibernate 7.0 auto-detects the dialect -->
        <!--<property name="dialect">
            org.hibernate.dialect.PostgreSQLDialect
        </property>-->


        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">create</property>

        <!-- Mapping files -->
        <mapping class="com.example.Person"/>

    </session-factory>

</hibernate-configuration>